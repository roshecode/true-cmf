:--input {
    min-width: 2rem;
    //padding: .4rem .6rem;
    padding: calc(1rem * 4 / 10) calc(1rem * 6 / 10);
    background: #525252;
    color: #d4d4d4;
    transition: box-shadow .2s;

    &:focus {
        box-shadow: 0 0 0 1px #888;
    }
}
