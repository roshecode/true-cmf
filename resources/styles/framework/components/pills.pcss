.pills {
    padding: .5rem;

    //background: #f1f1f1;
    background: #fcfcfc;
    color: #777;

    &.rounded {

    }

    & > ul {
        list-style: none;
        display: flex;
        flex-wrap: wrap;
        margin: -.25rem;

        & > li {
            margin: .25rem;
            padding: .25rem .5rem;
        }
    }
}

.pill {
    background: #f0f0f0;
}
